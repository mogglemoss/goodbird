# xeno-canto API key (free, get one at https://xeno-canto.org/account)
# Used by scripts/fetch-recordings.mjs to build the species manifest.
# If unset, the script falls back to the iNaturalist API (no key needed,
# variable quality).
XC_API_KEY=
